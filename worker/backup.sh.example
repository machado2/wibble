echo use linux or wsl to avoid encoding issues
echo
echo dumping database
# Create a PostgreSQL backup before maintenance.
pg_dump --format=custom --file=wibble.dump "$DATABASE_URL"

echo restoring database
# Restore into an empty PostgreSQL database when needed.
pg_restore --clean --if-exists --no-owner --dbname="$DATABASE_URL" wibble.dump
